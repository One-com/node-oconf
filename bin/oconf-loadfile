#!/usr/bin/env node
// ex: filetype=javascript

var oconf = require('../lib/index'),
    sys = require('sys'),
    optimist = require('optimist');

var argv = optimist
            .demand(1)
            .usage('Usage: $0 <filename.cjson>')
            .argv;

try {
    console.log(sys.inspect(oconf.load(argv._[0], 99, false)));
} catch (error) {
    console.error("Error:", error.message);
}
